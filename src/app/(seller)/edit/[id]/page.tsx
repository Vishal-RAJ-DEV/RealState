type EditPropertyPageProps = {
  params: {
    id: string;
  };
};

export default function EditPropertyPage({ params }: EditPropertyPageProps) {
  return <div>{/* TODO: Edit property page for {params.id} */}</div>;
}
